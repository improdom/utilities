SELECT
  TABLE_NAME,
  PARTITION_NAME,
  COLUMN_NAME,
  DICTIONARY_SIZE,
  USED_SIZE,
  SEGMENT_COUNT
FROM $SYSTEM.DISCOVER_STORAGE_TABLE_COLUMNS
WHERE TABLE_NAME = 'AggTable'
  AND PARTITION_NAME LIKE 'COB_2025-10-02%'  -- current CoB
ORDER BY PARTITION_NAME, DICTIONARY_SIZE DESC;
