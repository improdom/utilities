Gross_InstTIC = 
IF(
    ISINSCOPE('Position'[Alternate Instrument ID1]), 
    SUM('Position'[Abs]),  -- At child level, return Abs
    CALCULATE(
        SUM('Position'[Abs]),
        ALLSELECTED('Position'[Alternate Instrument ID1]) -- At parent level, sum all children
    )
)
